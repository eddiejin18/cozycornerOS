"use client";

import { CozyCornerOS } from "@/components/CozyCornerOS";

export default function Home() {
  return (
    <main
      style={{
        height: "100vh",
        width: "100vw",
        overflow: "hidden",
      }}
    >
      <CozyCornerOS />
    </main>
  );
}
